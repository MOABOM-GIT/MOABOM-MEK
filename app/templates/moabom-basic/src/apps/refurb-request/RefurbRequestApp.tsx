import { createPlaceholderApp } from '../_shared/PlaceholderAppShell';
import { refurbRequestAppMetadata } from './metadata';

export const RefurbRequestApp = createPlaceholderApp(refurbRequestAppMetadata);
