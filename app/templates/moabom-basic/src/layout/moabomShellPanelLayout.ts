/**
 * 셸 좌·우 패널 공통: 상단 SubTabBar 고정 슬롯 높이(px).
 * `absolute` 서브탭과 겹치지 않도록 스크롤 영역 `padding-top`에 동일 값 사용.
 */
export const MOABOM_SHELL_SUB_TAB_SLOT_PX = 65;

/** 좌측 패널 하단 네비 고정 슬롯 — 스크롤 영역 `padding-bottom` */
export const MOABOM_SHELL_LEFT_PANEL_BOTTOM_SLOT_PX = 75;

/** SubTabBar 슬라이딩 인디케이터 `duration-300` 과 동기 — settledTab 지연 */
export const MOABOM_SHELL_SUB_TAB_TRANSITION_MS = 300;

/** 우측 패널 알림 탭 — 첫 페이지·더보기 기준 건수 */
export const MOABOM_SHELL_NOTIFICATION_PANEL_PAGE_SIZE = 6;
