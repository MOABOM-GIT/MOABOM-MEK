/**
 * Moabom admin semantic surface presets.
 * Layout JSON / TS 소비측은 긴 Tailwind 나열 대신 이 상수·CSS 시맨틱 클래스를 쓴다.
 *
 * @see src/styles/ui-system/components.css (.admin-field-input)
 */

/** 단일 라인 텍스트 인풋 — hospital SaaS 등 admin 폼 SSOT */
export const MOABOM_ADMIN_FIELD_INPUT = 'admin-field-input';
