<?php

/*
|--------------------------------------------------------------------------
| Moabom System Web Routes (미래 web-scoped 확장 지점)
|--------------------------------------------------------------------------
|
| 본 파일은 의도적으로 비어 있다. `ModuleRouteServiceProvider` 가 자동 적용
| 하는 `modules/moabom-system/` prefix 가 적절치 않은 루트-스코프 라우트가
| 필요해질 때 직접 `SystemServiceProvider::boot()` 에서 등록한다.
*/
