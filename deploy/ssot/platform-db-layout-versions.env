# platform DB 에 마지막으로 동기화된 module layout 버전 마커 (filesystem SSOT 대비).
# layout-sync Job 성공 시 deploy/lib/layout-sync-hash.sh 가 갱신한다.
# 운영 DB가 구형이면 filesystem 버전과 달라 sync 가 강제된다.
admin_realtime_vm=1.0.2
